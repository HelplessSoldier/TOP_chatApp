const User = require("../../models/User");

function handleRejectFriendRequest(message, socket) {
  console.log("hi from handle reject friend request!");
}

module.exports = handleRejectFriendRequest;
